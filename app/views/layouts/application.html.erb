<!DOCTYPE html>
<html>
  <head>
    <%# タイトルタグ - content_forで個別ページのタイトルを設定可能、未設定時はデフォルトで"EduFinTech" %>
    <title><%= content_for(:title) || "EduFinTech" %></title>
    <%# レスポンシブデザイン対応のビューポート設定 %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%# iOSでフルスクリーン表示を可能にする設定 %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%# Androidでフルスクリーン表示を可能にする設定 %>
    <meta name="mobile-web-app-capable" content="yes">
    <%# CSRF対策のためのメタタグ %>
    <%= csrf_meta_tags %>
    <%# CSP(Content Security Policy)のためのメタタグ %>
    <%= csp_meta_tag %>

    <%# 個別ページで追加のhead要素を挿入可能 %>
    <%= yield :head %>

    <%# PWA(Progressive Web App)のマニフェストファイル設定 - 現在はコメントアウト %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# ファビコンとアプリアイコンの設定 %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# アプリケーションのスタイルシートを読み込み - Turboでの追跡を有効化 %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body class="min-h-screen flex flex-col">
    <%# ヘッダーセクション - ナビゲーションを含む %>
    <header class="bg-white shadow">
      <nav class="container mx-auto px-4 py-4">
        <div class="flex justify-between items-center">
          <%# サイトロゴ/ホームリンク %>
          <%= link_to "EduFinTech", root_path, class: "text-xl font-bold text-gray-800" %>
          <%# ユーザーメニュー - ログイン状態で表示が変化 %>
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <%# ログイン済みユーザー向けメニュー %>
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-gray-600 hover:text-gray-900" %>
              <%= link_to "プロフィール", edit_user_registration_path, class: "text-gray-600 hover:text-gray-900" %>
            <% else %>
              <%# 未ログインユーザー向けメニュー %>
              <%= link_to "ログイン", new_user_session_path, class: "text-gray-600 hover:text-gray-900" %>
              <%= link_to "新規登録", new_user_registration_path, class: "bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600" %>
            <% end %>
          </div>
        </div>
      </nav>
    </header>

    <%# メインコンテンツエリア %>
    <main class="flex-grow container mx-auto px-4 py-8">
      <%# フラッシュメッセージ - 成功通知 %>
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>
      <%# フラッシュメッセージ - エラー通知 %>
      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
          <span class="block sm:inline"><%= alert %></span>
        </div>
      <% end %>

      <%# 個別ページのコンテンツを挿入 %>
      <%= yield %>
    </main>

    <%# フッターセクション %>
    <footer class="bg-gray-800 text-white">
      <div class="container mx-auto px-4 py-8">
        <div class="text-center">
          <%# 動的な年表示のコピーライト %>
          <p>&copy; <%= Time.current.year %> EduFinTech. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>
